name: chat_group_app
description: A real-time group chat application built with Flutter and WebSocket.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  web_socket_channel: ^2.4.0
  flutter_bloc: ^8.1.3
  equatable: ^2.0.7
  intl: ^0.18.1
  emoji_picker_flutter: ^1.6.3
  flutter_dotenv: ^5.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    # - .env 